# FSD-nityendra_panwar_B
nityendra singh panwar
AIM
Design and develop an interactive user interface using React.
Key Features
Component-based – UI is divided into reusable components
Virtual DOM – Faster updates and better performance
Declarative – You describe what UI should look like, React handles updates
Reusable code – Components can be reused across the application

The application will open at http://localhost:3000
